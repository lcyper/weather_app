abstract class WeatherRepository {
  // void byQuery=(args) {};
  Future<Map> byQuery(String city) => Future(() => {});
  // fromJson(Map json) {} //TODO: implementar esto

  // Future? _byLocationId(){}

  // Function byQuery = (){};
  // Function byLocationId = (){};
}
